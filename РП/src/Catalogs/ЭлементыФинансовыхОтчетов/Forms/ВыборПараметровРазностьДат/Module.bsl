

#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

	Если Параметры.Свойство("АвтоТест") Тогда // Возврат при получении формы для анализа.
		Возврат;
	КонецЕсли;
	
	ЕстьПериод = Справочники.ЭлементыФинансовыхОтчетов.ЭлементРазвернутПоПериоду(Параметры);
	Если Не ЕстьПериод Тогда
		Индекс = Элементы.НачалоПериода.СписокВыбора.НайтиПоЗначению("[Период группировки]");
		Элементы.НачалоПериода.СписокВыбора.Удалить(Индекс);
		Индекс = Элементы.КонецПериода.СписокВыбора.НайтиПоЗначению("[Период группировки]");
		Элементы.КонецПериода.СписокВыбора.Удалить(Индекс);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура Выбрать(Команда)
	
	Если ПроверитьЗаполнение() Тогда
		Результат = Новый Структура(
			"НачалоПериода, КонецПериода, Периодичность", 
			НачалоПериода, КонецПериода, Периодичность);
			
		Закрыть(Результат);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти
