
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АвтоТест") Тогда
		Возврат;
	КонецЕсли;
	
	ОтборСписка = "";
	Если Параметры.Отбор.Свойство("УчетнаяПолитика") Тогда
		ОтборСписка = "УчетнаяПолитика";
	ИначеЕсли Параметры.Отбор.Свойство("Организация") Тогда
		ОтборСписка = "Организация";		
	КонецЕсли;
	УстановитьВидимостьКолонок(ОтборСписка);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Процедура УстановитьВидимостьКолонок(ИмяКолонки)
	
	Если НЕ ПустаяСтрока(ИмяКолонки) Тогда
		Элементы[ИмяКолонки].Видимость = Ложь;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти
